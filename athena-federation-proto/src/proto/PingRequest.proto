syntax = "proto3";

package com.amazonaws.athena.connector.lambda.proto.request;
import "FederatedIdentity.proto";

option java_multiple_files = true;

message PingRequest {
  optional com.amazonaws.athena.connector.lambda.proto.security.FederatedIdentity identity = 1;
  optional string catalogName = 2;
  optional string queryId = 3;
}
